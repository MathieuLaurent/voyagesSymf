{% extends 'base.html.twig' %}

{% block title %}Edition de catégorie{% endblock %}

{% block body %}
<section class="py-5">
    <div class="container">

            <div class="card">
                <div class="card-body">
                    <h2 class="text-center">Catégorie n° {{ category.id }} : {{ category.name }}</h2>
                    <hr>
                <p class="card-text">
                    {{ include('admin/admin_category/_form.html.twig', {'button_label': 'Mise à jour'}) }}
                </p>
            </div>
        </div>
        </div>
    </div>
</section>



<div class="text-center">

    <a href="{{ path('admin_category_index') }}"><button type="button" class="btn btn-success btn-sm m-2">Retour à la liste des catégories</button></a>

    {{ include('admin/admin_category/_delete_form.html.twig') }}

</div>
{% endblock %}
